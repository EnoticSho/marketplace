spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: "jdbc:mysql://localhost:8000/marketplace?autoreconnect=true"
    password: Password123!
    username: root
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  flyway:
    baseline-on-migrate: true
    url: jdbc:mysql://localhost:8000/
    schemas: marketplace
    user: root
    password: Password123!